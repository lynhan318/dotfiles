;; helix.scm — loaded before init.scm by the steel build; functions provided
;; here become typed commands (invocable with `:`). Custom commands go here.
;; Plugin setup and keybindings live in init.scm.
