local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
	topmost = "window",
	height = 36,
	color = 0xff1e1e2e,
	padding_right = 2,
	padding_left = 2,
	margin = 0,
	corner_radius = 0,
	y_offset = 0,
	blur_radius = 0,
	sticky = "on",
})
